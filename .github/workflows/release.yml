name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          # macOS — CGO enabled, includes tray
          - name: darwin-arm64
            os: macos-latest
            goos: darwin
            goarch: arm64
            cgo: "1"
            asset: agent-daemon-darwin-arm64

          - name: darwin-amd64
            os: macos-15-intel
            goos: darwin
            goarch: amd64
            cgo: "1"
            asset: agent-daemon-darwin-amd64

          # Linux — CGO enabled on native amd64, disabled for arm64 cross-compile
          - name: linux-amd64
            os: ubuntu-latest
            goos: linux
            goarch: amd64
            cgo: "1"
            asset: agent-daemon-linux-amd64

          - name: linux-arm64
            os: ubuntu-latest
            goos: linux
            goarch: arm64
            cgo: "0"
            asset: agent-daemon-linux-arm64

          # Windows — CGO disabled (tray excluded)
          - name: windows-amd64
            os: windows-latest
            goos: windows
            goarch: amd64
            cgo: "0"
            asset: agent-daemon-windows-amd64.exe

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install Linux CGO deps (amd64)
        if: matrix.os == 'ubuntu-latest' && matrix.cgo == '1'
        run: sudo apt-get update && sudo apt-get install -y --fix-missing gcc libayatana-appindicator3-dev

      - name: Build
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: ${{ matrix.cgo }}
        run: |
          mkdir -p dist
          go build \
            -ldflags "-X github.com/ms/agent-daemon/internal/api.Version=${{ github.ref_name }} -s -w" \
            -o dist/${{ matrix.asset }} \
            ./cmd/agent-daemon/

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.asset }}
          path: dist/${{ matrix.asset }}
          retention-days: 1

  sign-macos:
    name: Sign & Notarize macOS (${{ matrix.arch }})
    needs: build
    runs-on: macos-latest
    strategy:
      matrix:
        include:
          - arch: arm64
            asset: agent-daemon-darwin-arm64
          - arch: amd64
            asset: agent-daemon-darwin-amd64
    steps:
      - uses: actions/checkout@v6

      - name: Download darwin artifact
        uses: actions/download-artifact@v8
        with:
          name: ${{ matrix.asset }}
          path: dist

      - name: Make binary executable
        run: chmod +x dist/${{ matrix.asset }}

      - name: Package, sign, notarize, create DMG
        env:
          APPLE_CERTIFICATE_P12: ${{ secrets.APPLE_CERTIFICATE_P12 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: |
          VERSION="${{ github.ref_name }}"
          bash packaging/macos/package.sh \
            dist/${{ matrix.asset }} \
            ${{ matrix.arch }} \
            "$VERSION"

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v7
        with:
          name: agent-daemon-darwin-${{ matrix.arch }}.dmg
          path: dist/agent-daemon-darwin-${{ matrix.arch }}.dmg
          retention-days: 1

  release:
    name: Create Release
    needs: [build, sign-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Make binaries executable
        run: |
          chmod +x dist/agent-daemon-linux-amd64 dist/agent-daemon-linux-arm64
          chmod +x dist/agent-daemon-darwin-arm64 dist/agent-daemon-darwin-amd64

      - name: Generate checksums
        working-directory: dist
        run: sha256sum * > checksums.txt && cat checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.5.0
        with:
          name: "agent-daemon ${{ github.ref_name }}"
          body: |
            ## agent-daemon ${{ github.ref_name }}

            ### Install

            **macOS / Linux**
            ```sh
            curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/main/install.sh | sh
            ```

            **Windows (PowerShell)**
            ```powershell
            irm https://raw.githubusercontent.com/${{ github.repository }}/main/install.ps1 | iex
            ```

            Then start the daemon:
            ```sh
            agent-daemon install && agent-daemon start
            # Web UI → http://localhost:7700
            ```

            ### What's included
            - macOS builds ship as a signed & notarized `.dmg` with the system tray app (`agent-daemon tray`)
            - All builds include the web UI at `http://localhost:7700`
            - Configure your AI provider (Anthropic / OpenAI) from the Settings tab in the UI
          files: |
            dist/agent-daemon-darwin-arm64
            dist/agent-daemon-darwin-amd64
            dist/agent-daemon-darwin-arm64.dmg
            dist/agent-daemon-darwin-amd64.dmg
            dist/agent-daemon-linux-amd64
            dist/agent-daemon-linux-arm64
            dist/agent-daemon-windows-amd64.exe
            dist/checksums.txt
          generate_release_notes: true
          fail_on_unmatched_files: true
