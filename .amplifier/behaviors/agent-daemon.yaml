bundle:
  name: agent-daemon-behavior
  version: 1.0.0
  description: Agent daemon skills and context — job scheduler control via CLI and HTTP API

tools:
  - module: tool-skills
    source: git+https://github.com/microsoft/amplifier-module-tool-skills@main
    config:
      skills:
        - "git+https://github.com/kenotron-ms/agent-daemon@main#subdirectory=.amplifier/skills"

context:
  include:
    - agent-daemon:context/agent-daemon-awareness.md
