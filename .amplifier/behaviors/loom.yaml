bundle:
  name: loom-behavior
  version: 1.0.0
  description: Loom skills and context — job scheduler control via CLI and HTTP API

tools:
  - module: tool-skills
    source: git+https://github.com/microsoft/amplifier-module-tool-skills@main
    config:
      skills:
        - "git+https://github.com/kenotron-ms/amplifier-app-loom@main#subdirectory=.amplifier/skills"

context:
  include:
    - loom:context/loom-awareness.md
