# Build output
dist/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Go
*.test
*.out
vendor/

# Environment / secrets
.env
*.env
keys.env
packaging/macos/setup-github-secrets.sh
.superpowers/

# Git worktrees (isolated feature branch directories)
# Prevents accidentally committing worktree contents to the repository
.worktrees/

# Canvas worktree sessions
canvas_worktrees/

# Frontend build artifacts
web/dist/
ui/dist/
ui/node_modules/

# TypeScript incremental build cache (regenerated on every tsc -b run)
*.tsbuildinfo
